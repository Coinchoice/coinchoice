import { Subject } from 'rxjs';

/* eslint-disable @typescript-eslint/no-unused-vars */
const getBucket = (bucketName, areaName) => ({
    get: jest.fn(),
    set: jest.fn(),
    update: jest.fn(),
    remove: jest.fn(),
    clear: jest.fn(),
    getKeys: jest.fn(),
    changeStream: new Subject(),
    valueStream: new Subject(),
});
const useBucket = (areaName, bucketName) => getBucket();
/**
 * Buckets for each storage area.
 */
const storage = {
    local: getBucket(),
    sync: getBucket(),
    managed: getBucket(),
};

export { getBucket, storage, useBucket };
//# sourceMappingURL=jest-mock-esm.js.map
