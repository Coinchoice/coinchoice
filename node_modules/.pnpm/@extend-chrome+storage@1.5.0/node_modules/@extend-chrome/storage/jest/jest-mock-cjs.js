'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var rxjs = require('rxjs');

/* eslint-disable @typescript-eslint/no-unused-vars */
const getBucket = (bucketName, areaName) => ({
    get: jest.fn(),
    set: jest.fn(),
    update: jest.fn(),
    remove: jest.fn(),
    clear: jest.fn(),
    getKeys: jest.fn(),
    changeStream: new rxjs.Subject(),
    valueStream: new rxjs.Subject(),
});
const useBucket = (areaName, bucketName) => getBucket();
/**
 * Buckets for each storage area.
 */
const storage = {
    local: getBucket(),
    sync: getBucket(),
    managed: getBucket(),
};

exports.getBucket = getBucket;
exports.storage = storage;
exports.useBucket = useBucket;
//# sourceMappingURL=jest-mock-cjs.js.map
